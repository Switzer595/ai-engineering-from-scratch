# Docker for AI Notes

## 当前状态

本机 Windows 版本为 Windows 10 21H1，Docker Desktop 4.75.0 要求至少 Windows 10 22H2 / build 19045。

因此本节 Docker 实操暂时跳过，等待 Windows 升级完成后继续。

## 待完成

- 安装 Docker Desktop
- 运行 `docker --version`
- 运行 `docker run hello-world`
- 编写 Dockerfile
- 构建并运行 Docker 镜像